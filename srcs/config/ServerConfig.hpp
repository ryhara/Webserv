#pragma once

#include "Webserv.hpp"

class ServerConfig
{
	private:
		// TODO : server１つ分の情報を保持する
		DISALLOW_COPY_AND_ASSIGN(ServerConfig);
	public:
		ServerConfig();
		~ServerConfig();

		// TODO : getter
		// TODO : setter
};